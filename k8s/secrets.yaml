# IMPORTANT: This is a template. DO NOT commit actual secrets to version control.
# Create secrets using: kubectl create secret generic codex-prime-secrets --from-literal=...

apiVersion: v1
kind: Secret
metadata:
  name: codex-prime-secrets
  namespace: codex-prime
type: Opaque
stringData:
  # Replace these with actual values
  OPENAI_API_KEY: "your-openai-api-key-here"
  ANTHROPIC_API_KEY: "your-anthropic-api-key-here"
  POSTGRES_PASSWORD: "your-secure-password-here"
  GRAFANA_PASSWORD: "your-grafana-password-here"
